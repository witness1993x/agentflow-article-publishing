我最初以为 subagent 就是 LLM 版线程池。3 月底我照这个直觉拼出一个 6 节点的 agent,跑真实任务时端到端反而慢了 30%。

真正的代价不是计算,是上下文切换。每次子 agent 接过任务都要重建一部分上下文,这份"重入成本"要跑 20 次才显形。subagent 不是免费的加速,它是一种有代价的抽象。

后来我换了思路:不把 subagent 当成"让 LLM 并行",而是"让推理单位变小"。视角一换,判断依据从"能不能并发"变成"这块推理是不是独立的意图",我后面所有 agent 架构的起点都改写了。
