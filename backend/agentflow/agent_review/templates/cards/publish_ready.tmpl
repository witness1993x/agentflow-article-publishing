📤 *Ready to publish*  ·  `{short_id}`  ·  {timestamp}

*{title}*
{subtitle_line}━━━━━━━━━━━━━━━━━━━━━━━━
publisher: *{publisher_brand}*
tags: {tags_joined}
{canonical_line}━━━━━━━━━━━━━━━━━━━━━━━━
{warnings_block}━━━━━━━━━━━━━━━━━━━━━━━━
package: `{package_path}`

下一条消息附 `package\.md`, 直接拷进 Medium 编辑器即可\.
